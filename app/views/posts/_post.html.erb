<div id="<%= dom_id post %>">
  <p>
    <strong>Title:</strong>
    <%= post.title %>
  </p>

  <p><%= post.views%> views</p>
    <p>Posted by <%= link_to post.user.name, user_path(post.user)%></p>

  <p>
    <strong>Body:</strong>
    <%= post.body %>
  </p>

</div>
